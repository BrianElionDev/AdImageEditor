[phases.setup]
nixpkgs = [
  "pkg-config",
  "cairo",
  "libpng",
  "pango",
  "giflib",
  "jpeg",
  "fontconfig",
  "freetype",
  "noto-fonts-emoji",
  "noto-fonts",
  "fontconfig"
]

[phases.install]
cmds = [
  "pnpm install",
  "fc-cache -fv"
]

[phases.build]
cmds = [
  "pnpm rebuild canvas"
]

[start]
cmd = "pnpm start" 