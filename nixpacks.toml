[phases.setup]
nixpkgs = [
  "pkg-config",
  "cairo",
  "libpng",
  "pango",
  "giflib",
  "jpeg",
  "fontconfig",
  "freetype"
]

[phases.build]
cmds = [
  "pnpm rebuild canvas --update-binary"
]

[start]
cmd = "pnpm start" 